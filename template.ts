import solve from "@/solve";
import { stripIndents } from "common-tags";
import { chain } from "lodash";

solve({
  part1: {
    tests: [
      {
        input: stripIndents``,
        expected: 0,
      },
    ],
    fn: (input) => {
      return 0;
    },
  },
  part2: {
    tests: [
      {
        input: stripIndents``,
        expected: 0,
      },
    ],
    fn: (input) => {
      return 0;
    },
  },
});
